Kody do scrape'owania danych znajdują się w folderze danych "DANE". Wystarczy wtedy mieć plik AllDocuments.xlsx i następnie uruchomić kody w kolejnosci:
1.get_data.py
2.extract_data.py
3.extract_sri.py

Wszystkie pobrane dokumenty trafiły do folderu KID_pdfs

GŁÓWNA CZĘŚĆ PROJEKTU:
W celu uruchomienia projektu wystarczy uruchomić kody w komórkach z pliku UGPT_PROJEKT.ipynb
Zalecamy uruchomić to w środowisku google colab (bo my w tym środowisku to stworzyliśmy), ale
mozna tez notatnik uruchmomić np. w VISUAL STUDIO.

Dalsze uruchomienie dla google colab - wazne aby upewnić się, ze dane znajdują się w odpowiedniej lokalizacji. U nas jest to:
PATH = '/content/drive/MyDrive/PROJEKT/dataset_final.csv'

Jest to ewentualnie jedyna rzecz do zmiany.

